A Python-based real-time tutor-student attendance system using sockets and Tkinter that displays student ID, name, and port, and auto-updates attendance as students join or leave.

To run this file, navigate to command prompt > downloaded file > run it using the command "python 'filename'.py"
